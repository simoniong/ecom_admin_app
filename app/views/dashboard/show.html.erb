<div class="max-w-4xl">
  <h1 class="text-2xl font-semibold text-gray-900"><%= t("dashboard.title") %></h1>
  <p class="mt-2 text-sm text-gray-600"><%= t("dashboard.welcome", email: current_user.email) %></p>
</div>
